<Window
    x:Class="MyWarehouse.Windows.AddEditTaskWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MyWarehouse.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Добавить задачу"
    Width="420"
    Height="300"
    mc:Ignorable="d">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            VerticalAlignment="Center"
            Text="Продукт:" />
        <ComboBox
            x:Name="ProductCombo"
            Grid.Row="0"
            Width="280"
            Margin="90,0,0,0" />

        <TextBlock
            Grid.Row="1"
            VerticalAlignment="Center"
            Text="Клиент:" />
        <ComboBox
            x:Name="ClientCombo"
            Grid.Row="1"
            Width="280"
            Margin="90,0,0,0" />

        <TextBlock
            Grid.Row="2"
            VerticalAlignment="Center"
            Text="Количество:" />
        <TextBox
            x:Name="QtyBox"
            Grid.Row="2"
            Width="120"
            Margin="90,6,0,6" />

        <TextBlock
            Grid.Row="3"
            VerticalAlignment="Top"
            Text="Тип доставки:" />
        <ComboBox
            x:Name="DeliveryTypeCombo"
            Grid.Row="3"
            Width="280"
            Margin="90,0,0,0" />

        <StackPanel
            Grid.Row="4"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Width="90"
                Margin="6"
                Click="Cancel_Click">
                Отмена
            </Button>
            <Button
                Width="90"
                Margin="6"
                Click="Ok_Click">
                Добавить
            </Button>
        </StackPanel>
    </Grid>
</Window>
